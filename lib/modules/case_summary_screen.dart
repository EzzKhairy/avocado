import 'package:flutter/material.dart';

class CaseSummary extends StatelessWidget {
  const CaseSummary({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Case info scre')),
    );
  }
}
